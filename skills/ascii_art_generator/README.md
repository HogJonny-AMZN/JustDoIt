# ascii_art_generator Skill Documentation

## Overview
This skill generates ASCII art using the 'JustDoIt' tool and saves it to a file.

## Usage
1. Install dependencies if necessary.
2. Run the skill with command line parameters:

```
oopenclaw run skills/ascii_art_generator main.py --text '<your_text>' --output /path/to/output.txt
```