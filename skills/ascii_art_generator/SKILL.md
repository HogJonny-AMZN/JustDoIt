# ascii_art_generator

Generates ASCII art using the 'JustDoIt' tool.

## Usage
1. Install dependencies (if any).
2. Run the skill with specified input parameters.
3. Check the output file for generated ASCII art.

## Example Command
```
oopenclaw run skills/ascii_art_generator main.py --text 'Hello World' --output /path/to/output.txt
```